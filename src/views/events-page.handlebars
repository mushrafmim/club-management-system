<div class="py-3 d-flex align-items-center justify-content-end">
  <a role="button" class="btn btn-primary" href="events/new">NEW EVENT</a>
</div>
<table class="table">
  <thead>
    <tr>
      <th scope="col">id</th>
      <th scope="col">Title</th>
      <th scope="col">Date</th>
      <th scope="col">Venue</th>
    </tr>
  </thead>
  <tbody>
    {{#each allEvents}}
      <tr>
        <th scope="row">{{id}}</th>
        <td>{{title}}</td>
        <td>{{date}}</td>
        <td>{{venue}}</td>
        {{!-- <td>
          {{#if birthday_scheduled}}
            SCHEDULED
          {{else}}
            <a role="button" href="/events/schedule/{{id}}" class="btn btn-outline-success btn-sm">SCHEDULE</a>
          {{/if}}
          <div class="dropdown">
            <i class="bi bi-three-dots-vertical" type="button" data-bs-toggle="dropdown" aria-expanded="false"></i>
            <ul class="dropdown-menu">
              <li><a class="dropdown-item" href="members/view/{{id}}">VIEW</a></li>
              <li><a class="dropdown-item text-warning" href="#">EDIT</a></li>
              <li><a class="dropdown-item text-danger" href="members/delete/{{id}}">DELETE</a></li>
            </ul>
          </div>
        </td> --}}
      </tr>
    {{/each}}
  </tbody>
</table>